{{#if hasBlock}}

  {{yield}}

{{else}}

  <button {{action 'dismiss'}} class='Ted-modal-footer__cancel-button'>Cancel</button>

  {{async-button action='sendPrimaryAction'
      class='Ted-modal-footer__primary-button'
      classBinding='danger:btn-danger'
      data-test-id=testId
      default=default
      pending=pending}}

{{/if}}
